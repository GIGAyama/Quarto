# GIGA Standard v5 監査：GIGAクアルト！

- **リポジトリ**：`GIGAyama/Quarto`
- **型**：B型（Vite + React、GitHub Pages）
- **監査日**：2026-08-05
- **比較対象**：`0e423df`（改修前）を**実際にチェックアウトしてビルドし、同じ道具で測ったもの**
- **測り方**：`tools/measure.mjs` / `tools/measure-update.mjs`（実ブラウザ・Chromium）

> **「未計測」は ✅ ではない。** 測っていないものは下の「測っていないもの」に全部書いてある。

---

## 実測のしかた（再現手順）

```bash
npm ci
npm run build
npm run serve:dist &            # dist/ を本番と同じ /Quarto/ の下で配る
npm i -D playwright             # このリポジトリの依存には入れていない（授業用アプリの npm ci を重くしないため）
npm run measure                 # 表示・コントラスト・タップ・CSP・SW・オフライン
npm run measure:update          # 更新の待機と切り替え
npm run check                   # 静的な品質ゲート
npm run check:self              # ゲートをわざと壊して、ちゃんと落ちることを見る
```

---

## A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT / Copyright (c) 2026 GIGAyama（新規作成） |
| A2 | .gitignore | ✅ | `node_modules/ dist/ dev-dist/ .clasp.json .env .env.* .DS_Store *.log` |
| A3 | dependabot.yml | ✅ | monthly（npm + github-actions）。メジャー更新は自動で上げない |
| A4 | README / MANUAL / AUDIT | ✅ | 3つとも用意（MANUAL.md・AUDIT.md は新規） |
| A5 | CI が pull_request でも動く | ✅ | `.github/workflows/ci.yml` を新設（lint → test → build → check） |
| A6 | `npm audit` | ⚠️ | **10件 → 3件**。残り3件（vite high / esbuild moderate / vite-plugin-pwa moderate）は**メジャー更新が必要**なので `--force` を使わず据え置いた。**停止条件に該当するため人間の判断を仰ぐ**（下記） |

`npm audit` の残り3件はいずれも **vite の開発サーバーの脆弱性**で、GitHub Pages に置かれる成果物には含まれない。
ただし GIGA Standard v5 の停止条件「`npm audit` が高危険度で落ち、マイナー更新で解消できないとき」に当たるため、
**vite 5 → 8 への更新は行わず、人間の判断に委ねる。**

## B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（入れたうえで動作確認済み） | ✅ | **新規投入。** 実ブラウザ3画面幅×6画面で **CSP違反 0件・JSエラー 0件**。改修前は CSP そのものが無かった（＝「違反0件」に意味は無い） |
| B2 | 秘密情報・IDの直書きなし | ✅ | `.env` / `.clasp.json` なし。APIキー・メールアドレスの直書きなし |
| B3 | OAuthスコープ最小 | ➖ | 対象外（GAS を使っていない） |
| B4 | postMessage の宛先が `*` でない | ✅ | `postMessage` の使用は Service Worker への `{type:'SKIP_WAITING'}` のみ（宛先は同一オリジンの SW） |
| B5 | サーバー側5段ガード | ➖ | 対象外（サーバーが無い。個人情報を一切扱わない） |
| B6 | **CDN から取る実行コードが 0** | ✅ | **改修前から 0 バイト。** `@babel/standalone` / `cdn.tailwindcss.com` / unpkg / jsDelivr のいずれも無し。React・three.js・SweetAlert2・canvas-confetti はすべて npm からバンドル |
| B7 | 残る外部資産に SRI と版の固定 | ➖ | 対象外（外部から読む実行コードが無い）。Google Fonts のみ外部だが**見た目だけの依存**（§2-7）で、塞がれても端末側フォントに落ちる |

`frame-ancestors` は `<meta>` に**書いていない**（配っても無視され、警告が出るだけになる）。
独自ドメインや CDN を挟むときに HTTP ヘッダーで設定するよう `index.html` のコメントに残した。

`script-src 'self'` を入れるにあたり、`beforeinstallprompt` の捕捉は
インラインではなく `public/install-hook.js` という外部ファイルにしてある。

## C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService | ➖ | 対象外（GAS を使っていない） |
| C2 | 自動復旧 | ➖ | 対象外（サーバー側の保存が無い） |
| C3 | pagehide で記録確定 | ➖ | 対象外（**保存する記録が無い**。対局結果を保存しない作りなので、Chromebook のタブ破棄で失われるものが無い） |
| C4 | 通信失敗時のリトライと明示 | ✅ | アプリは起動後に通信しない。圏外時は Service Worker が手元の控えを返し、控えも無ければ `offline.html` を出す（実測済み） |
| C5 | localStorage.clear() を使っていない | ✅ | `localStorage` 自体を使っていない |

## D. 表示（実ブラウザで 320×568 / 375×667 / 1366×768 を走査）

| # | 項目 | 判定 | 実測（前 → 後） |
|---|---|:--:|---|
| D1 | viewport に viewport-fit=cover | ✅ | 改修前からあり |
| D2 | 100dvh を使用 | ✅ | `height: 100%` → **`100dvh`**（`@supports not` で `100vh` にフォールバック） |
| D3 | safe-area-inset | ✅ | 上下のみ → **上下＋左右**（横向きでノッチ側が欠けないように） |
| D4 | clamp() による fluid type | ✅ | **無し → 4段階**（`--fs-body` / `--fs-lead` / `--fs-title` / `--fs-small`） |
| D5 | Canvas に devicePixelRatio 補正（上限2） | ✅ | 改修前からあり（`renderer.setPixelRatio(Math.min(devicePixelRatio, 2))`）。画面回転にも追従 |
| D6 | 320px 幅で横スクロールが出ない | ✅ | 前 0px → 後 **0px**（どちらも問題なし） |
| D7 | 画像に width/height、150KB以下 | ➖ | 対象外（`<img>` が1つも無い。盤面は WebGL、しるしは絵文字とCSS図形） |
| D8 | **コントラスト 4.5:1 以上** | ✅ | **11種類が基準未満 → 0件**（下表） |
| D9 | **タップ領域 44px 以上** | ✅ | **6種類が44px未満 → 0件**（下表） |
| D10 | prefers-reduced-motion（.01ms） | ✅ | **無し → あり**。`.01ms` であって `0` ではない |
| D11 | forced-colors 対応 | ✅ | **無し → あり**（ボタンと更新の帯に枠線） |
| D12 | 提示モード | ✅ | **無し → あり**（「🔲 大きく」で 150%＋全画面。ヘッダーが折り返す 640px 未満では出さない） |
| D13 | 印刷CSS | ➖ | 対象外（印刷して使うアプリではない）。誤って印刷したときに操作パネルや更新の帯が紙に出ないだけの指定は入れた |
| D14 | 拡大を禁止していない | ✅ | 改修前から `user-scalable=no` なし |

### D8 コントラスト（すべて実ブラウザでの実測値）

**アプリ固有の配色より先に、ライブラリの既定色が基準に届いていなかった。**
11種類のうち **6種類が SweetAlert2 の既定色**である。

| 出どころ | どこの文字か | 前 | 後 |
|---|---|---:|---:|
| アプリ | 「？」ボタン（白文字 on `#ffca28`） | **1.53** | 6.09 |
| アプリ | プレイヤー記号 `●`（`#ff7043`） | 4.05 | 8.26 |
| アプリ | 「最初から」ボタン（`text-red-500`） | 3.76 | 5.62 |
| アプリ | 「最初から」の**ふりがな**（同上を継承） | 3.76 | 5.62 |
| アプリ | ポップアップ内のふりがな（`rt { color: #8d6e63 }`） | **4.31** | 7.1（白地の既定値） |
| アプリ | 「わかった！」ボタン（白文字 on `#4caf50`） | **2.78** | 5.33 |
| SweetAlert2 | `swal2-question` の `?`（`#87adbd`） | **2.24** | 6.5 |
| SweetAlert2 | `swal2-info` の `i`（`#3fc3ee`） | **1.91** | 5.2 |
| SweetAlert2 | `swal2-warning` の `!`（`#f8bb86`） | **1.58** | 3.9 |
| SweetAlert2 | `swal2-success` の ✓ の線（`#a5dc86`／図形なので基準は3:1） | **1.49** | 4.8 |
| SweetAlert2 | `swal2-error` の × の線（`#f27474`／同上） | **2.60** | 5.1 |
| SweetAlert2 | 「いいえ」ボタン（白文字 on `#3085d6`） | **3.85** | 5.7 |
| Google Material | 見出し・ボタンの青 `#1a73e8` | 4.27（表も裏も届いていない） | 6.54（`#1967d2`） |

**ふりがなの色は決め打ちをやめた。** 白地の既定値だけを決め、
ボタン・バッジ・色のついた面の上では `color: inherit` で継がせる。
1か所ずつ潰すと、気づいた場所だけ直って他が漏れる。

**勝ち・引き分けのポップアップは対局を最後まで進めないと出ない**ので、
開いているポップアップの中に同じしるしを実際に差し込んで測った（`tools/measure.mjs`）。
色はライブラリのスタイルシートから計算されるため、本番と同じ値になる。

### D9 タップ領域（`::after` 込みの実効サイズ）

| どのボタンか | 前 | 後 |
|---|---:|---:|
| サウンド ON/OFF | 60.3 × **28** | 44以上 |
| ルール「？」 | **36 × 36** | 44以上 |
| 「最初から」 | 102.1 × **35.2** | 44以上 |
| フッターの「GIGA山」リンク | **39.2 × 12** | 44以上 |
| ポップアップの「つぎへ ▶」 | 117.6 × **40** | 44以上 |
| ポップアップの「わかった！」 | 129.5 × **40** | 44以上 |

ヘッダーは詰めて組んであるので、ボタン自体を大きくすると折り返して別の破綻を生む。
**疑似要素（`.tap-44::after`）で当たり判定だけを広げた。**
ポップアップのボタンは場所に余裕があるので `min-height: 44px` で素直に大きくした。

## E. PWA

| # | 項目 | 判定 | 実測（前 → 後） |
|---|---|:--:|---|
| E1 | manifest の id/scope/start_url がリポジトリ名絶対パス | ✅ | `scope`/`start_url` は前からあり。**`id` が無かった** → `/Quarto/` を明示 |
| E2 | アイコン4種 + 透明を含まない apple-touch-icon | ✅ | maskable-192 が**無かった** → 追加。apple-touch-icon に**透明があった（alpha 最小 0）** → 下地で塗りつぶし、**透明 0画素** |
| E3 | beforeinstallprompt を head 最上部で捕捉（外部ファイル） | ✅ | **捕まえていなかった** → `public/install-hook.js` を `<head>` の先頭で同期読み込み |
| E4 | インストールボタン（案内できるときだけ表示） | ✅ | **無かった** → ヘッダーに「⬇ いれる」。合図が来たときだけ出す。すでにインストール済み（standalone）なら出さない |
| E5 | sw.js が自アプリ接頭辞のキャッシュのみ削除（実測） | ✅ | 前：workbox が URL 単位で名前を分けており巻き添えは無かった（実測で確認）。後：`giga-quarto-` 接頭辞で明示的に絞る。**別アプリ名のキャッシュ2つを置いて版を上げ、両方残ることを実測** |
| E6 | sw.js が localStorage に触れていない | ✅ | 前後とも触れていない |
| E7 | **更新は押すまで切り替わらない** | ✅ | **前：4秒放置で `waiting: false`（誰も押していないのに新しい版が有効化されていた）** → **後：3秒放置で `waiting: true` のまま**。「さいしんに する」を押して初めて切り替わることも実測 |
| E8 | 初回訪問で勝手にリロードしない | ✅ | 前 **画面遷移1回** → 後 **1回**（どちらも問題なし。`controllerchange` は利用者が押したときだけ受ける形にしてある） |
| E9 | Service Worker が実際に登録されている | ✅ | `getRegistration()` で `active: true` を確認。登録は React の外（`src/pwa.js`）で行い、**`readyState` の分岐**を入れてある |
| E10 | offline.html（外部資産・JS に頼らない） | ✅ | **前：本体の控えを消すと何も出なかった** → 後：`offline.html` が出ることを実測。`<script>` も外部URLも持たない |
| E11 | SW の版が配信物の中身から自動で作られる | ✅ | `tools/build-sw.mjs` が `dist/sw.js` に刻む。手書きに戻す／生成器を消す／`build` の配線を外す、のどれでも品質ゲートが止める（2026-08-22 に自動化。それまでは手書きで、`quality.config.json` の `appVersion` といっしょに上げ忘れれば揃ったまま緑だった） |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ✅ | 512：**0.012%** / 192：**0.024%**（画素を数えて実測。下地は切り抜かれてよいので中身と色で区別した） |
| E13 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ✅ | MANUAL.md に記載 |

Service Worker は `vite-plugin-pwa` の生成任せ（`generateSW` + `registerType: 'autoUpdate'`）をやめ、
**`injectManifest` に切り替えて `src/sw.js` を Part I §3-3 の形で自分で書いた。**
先読みするファイルの一覧だけをビルド時に差し込ませている（ハッシュ付きのファイル名を手で並べると必ず嘘になるため）。

> **`id` を足しても、すでにインストール済みの端末で別アプリ扱いにはならない。**
> `id` を省略したときの既定値は `start_url`（＝`/Quarto/`）なので、明示しても値は変わらない。

## F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ✅ | `<img>` は無い。アイコンのみのボタンに `aria-label`、中の記号に `aria-hidden`。**手番の案内に `aria-live="polite"` を追加**（盤面の変化を目で追えない子にも届くように） |
| F2 | モーダルに role="dialog"・Esc で閉じる | ✅ | SweetAlert2 が `role="dialog" aria-modal="true"` とフォーカス閉じ込めを持つ。**Esc で閉じることを実ブラウザで実測** |
| F3 | キーボードのみで全機能に到達 | ⚠️ | ヘッダー・操作パネル・ポップアップはすべて Tab で到達でき、`:focus-visible` の枠も出る。**ただし盤面のコマの選択・配置は 3D 画面のタップ／クリックのみで、キーボードの代替手段が無い**（下記「人間に確認してほしいこと」） |
| F4 | rt の色を決め打ちしていない | ✅ | `#8d6e63` の決め打ち → 白地の既定値＋色のついた面では `inherit` |
| F5 | 初回JS 300KB以下 | ❌ | **702KB（gzip前）／196KB（gzip後）。基準を満たしていない**（下記） |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大は `src/App.jsx` の 約780行。中核ロジックは `src/game.js` に切り出した |

### F5 が満たせない理由（既知の逸脱）

| 内訳 | gzip前 | gzip後 |
|---|---:|---:|
| `three.js` | 451.4 KB | 114.4 KB |
| `react` + `react-dom` | 140.9 KB | 45.3 KB |
| アプリ本体（SweetAlert2・canvas-confetti 含む） | 125.4 KB | 37.1 KB |
| **合計** | **702 KB** | **196 KB** |

3D の盤面が本アプリの中身そのものなので、`three.js` を外すには 3D をやめるしかない。
**CDN からは 1バイトも取っておらず**（§8 の「CDN から取る実行コード 0バイト」は満たしている）、
すべて自己ホストして先読みキャッシュに入れてあるため、2回目以降は通信が発生しない。
`quality.config.json` の `knownDeviations` に理由を書いてあり、`npm run check` は毎回この数字を表示する。
**3D をやめるかどうかは人間の判断に委ねる。**

### 総アセット（初回）

| | 前 | 後 |
|---|---:|---:|
| 先読みするファイルの合計 | **1947.0 KB** | **767.4 KB** |

減った分はほぼアイコンである。

| ファイル | 前 | 後 |
|---|---:|---:|
| `favicon.png`（1024×1024 フルカラー → 256×256 パレット） | 679.0 KB | **5.2 KB** |
| `pwa-512x512.png` | 299.3 KB | **15.8 KB** |
| `maskable-512x512.png` | 190.4 KB | **11.2 KB** |
| `pwa-192x192.png` | 50.8 KB | **3.6 KB** |
| `apple-touch-icon.png` | 45.1 KB | **3.4 KB** |
| `maskable-192x192.png` | （無し） | **3.1 KB** |
| **合計** | **1264.6 KB** | **42.3 KB** |

元の絵は色数の少ないドット絵なので、フルカラーで持つ理由がない。32色のパレット PNG にした。
1024×1024 の原本は `assets/icon-master.png` に置いてあり、**配布物には含めない**。

## G. 学習ログ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1 | study.v1 準拠 | ➖ | 対象外（対局の記録を一切保存しない作り。`localStorage` を使っていない） |
| G2 | 中断記録・5分ルール | ➖ | 対象外（同上） |

---

## 品質ゲート

`npm run check` … 満たした 27 / 満たしていない 0 / 既知の逸脱 1（F5）/ 対象外 0

`npm run check:self` … **29/29 の検査が、わざと壊したときに落ちることを確認済み。**

> 「0件でした」だけでは、検査が動いているのか何も見ていないのか区別できない。
> 実際、この確認をしたうえで実リポジトリに当てたことで、**検査そのものの不具合が4件**見つかった。
>
> | 見つかった不具合 | 中身 |
> |---|---|
> | `D2_DVH` の誤検知 | 「`100vh` はモバイルのアドレスバー分だけはみ出す」という**注意書きのコメント**に反応していた。判定前にコメントを空にする（行番号は保つ） |
> | `E3_INSTALL_HOOK` の誤検知 | 同じく、`beforeinstallprompt` と書いてある**コメント**を「インラインで書かれている」と判定。`src` の無い `<script>` の中だけを見る |
> | `B1_CSP` の誤検知 | CSP の説明コメントに書いた `'unsafe-inline'` の字を拾って「script-src が緩い」と判定 |
> | ファイル一覧の取りこぼし・過剰 | 拡張子を部分一致で調べていたため `package-lock.json` が `.js` に一致し、逆に `dist/assets/` はディレクトリ名で除外されて見えていなかった |

`E5_SW_CACHE_SCOPE` は、削除式（`caches.delete(k)`）を追うのではなく
**「`startsWith` で絞る式があるか」**を見る形にしてある（`(k) => caches.delete(k)` を見落とさないため）。
`E6_SW_NO_LOCALSTORAGE` は、判定前にコメントを落としてから調べる
（「localStorage は操作しない」という注意書きに反応しないため）。

## テスト

`npm test` … 18件すべて通過。内訳は勝敗判定（`src/game.js`）と、
下の「アプリが開けない」で足した起動まわりの見張り（`tests/boot.test.js` /
`tests/recovery.test.js`）。

「そろっていないのに勝ちになる」より「**そろっているのに勝ちにならない**」ほうが授業では困る
（子どもが正しく並べたのに認めてもらえない）ので、両方向を確かめている。
また「背の高いコマを4枚」と素朴に選ぶと色まで同じものが並んでしまい、
高さの判定を試したつもりが色の判定を試すことになるため、
**その属性だけがそろっていること**をテストの中で先に確認してから判定にかけている。

起動まわりの3本は、いずれも「直す前の書き方に戻すと落ちる」ことを確認してある。

---

## 「アプリが開けない」（2026-08-23）

エラー画面が出てアプリが開けない、という報告を追ったところ、
**独立した3つ**の原因が見つかった。いずれも「即座に拒否される」環境では素通りし、
**学校のフィルタのように『握ったまま返さない』塞がれ方でだけ**表に出る／
出たあとに抜け出せなくなる、という性質を持っていた。

| # | 場所 | 症状 | 直し方 |
|---|---|---|---|
| 1 | `src/index.css` の `@import` | Google Fonts が無応答だと **React が永久に起動しない**。`<head>` の `<link rel="stylesheet">` が読み込み中の間、ブラウザは module script を実行しないため。画面には `index.html` の「うまく ひらけませんでした」だけが残り、押し直しても同じ壁に当たる | `@import` をやめ、`src/font.js` が `load` 後に `media="print"` → `all` で足す |
| 2 | `src/ErrorScreen.jsx` の scope 比較 | 「データを けして ひらきなおす」が **Service Worker を1つも解除できていなかった**。`base` が `'./'` なので `BASE_URL` も `'./'` になるが、`registration.scope` の `pathname` は `'/'`。`'/'.startsWith('./')` は必ず `false` になる。古い控えが出続け、**エラー画面から抜け出せない** | 比較の前に両方を絶対パスへ直す（`ownsScope()` を `src/app-cache.js` に置いた） |
| 3 | `tools/serve-dist.mjs` | `BASE` が `'/'` になったのに `/` → `BASE` の 302 が残っており、`ERR_TOO_MANY_REDIRECTS`。`tools/measure*.mjs` は `${ORIGIN}/` を開くので、**検査側が丸ごと動かなくなっていた** | `BASE !== '/'` のときだけ案内する |

1 は「フォントが届かなくても字の形が変わるだけ」という前提が崩れていた例である。
拒否が即座に返る手元では再現しないので、目視の動作確認では見つけられない。
`tests/boot.test.js` で機械に見張らせている。

実測（Chromium、`fonts.googleapis.com` を無応答にして `/` を開く）:

| | 直す前 | 直したあと |
|---|:--:|:--:|
| 盤面が出る | ❌（12秒後に「うまく ひらけませんでした」） | ✅ |
| Service Worker が登録される | ❌ | ✅ |
| オフラインで開ける | ❌ | ✅ |

---

## 測っていないもの

**以下は「未計測」であって、✅ ではない。**

| 何を | なぜ |
|---|---|
| **本番（`https://gigayama.github.io/Quarto/`）での動作** | 作業環境から到達できない（プロキシ 403）。測ったのはすべて `dist/` をローカルで `/Quarto/` の下に配ったもの |
| **iPad / iOS Safari の実機** | 実機が無い。`apple-touch-icon` の黒枠は「透明が 0画素であること」を画素で確認しただけで、iPad のホーム画面では見ていない |
| **Chromebook の実機・LCP・CLS** | 実機が無い。読み込むファイルの大きさは測ったが、実機の表示時間は測っていない |
| **電子黒板での提示モードの見え方** | 実機が無い。CSS とボタンの動作は確認したが、教室の後ろから読めるかは確かめていない |
| **「⬇ いれる」ボタンが実際に出るか** | headless の Chromium では `beforeinstallprompt` が発火しない。コードは §3-2 の形にしてあり、合図が来たときだけ出す作りであることはコード上確認できるが、**実際にボタンが出るところは見ていない** |
| **iOS の「共有 → ホーム画面に追加」** | 同上。MANUAL.md に手順を書いたが、実機で辿っていない |
| `prefers-reduced-motion` / `forced-colors` を有効にした実表示 | CSS が入っていることは確認したが、その設定を入れた状態の画面は見ていない |
| 40人が同時に開いたときの校内 Wi-Fi の挙動 | 測れない |
| Google Fonts が塞がれた状態での字形 | 作業環境からは `fonts.googleapis.com` に出られないため**塞がれた状態でしか測っていない**（＝学校と同じ条件）。逆に、**フォントが届いている状態は測っていない** |

---

## 人間に確認してほしいこと

1. **`npm audit` の残り3件（vite high ほか）を上げるか。**
   解消には vite 5 → 8 のメジャー更新が必要で、停止条件に当たるため据え置いた。
   3件とも開発サーバーの脆弱性で、GitHub Pages に置かれる成果物には含まれない。

2. **初回 JS 702KB（基準 300KB）をどうするか。**
   `three.js` を外すには 3D をやめるしかない。アプリの作り替えになるため判断を仰ぐ。

3. **盤面のキーボード操作。**
   コマの選択と配置は 3D 画面のタップ／クリックのみで、キーボードの代替手段が無い。
   §2-9 の「ドラッグ操作には必ずキーボード／ボタンの代替手段を用意する」に照らすと不足しているが、
   盤面の 4×4 と待機コマ16個を選ぶ UI を新たに作ることになり、100行を大きく超える。
   **どこまでやるかを決めてから別の PR にしたい。**

4. **Service Worker の作り替え（`generateSW` → `injectManifest`）。**
   更新を押すまで待たせるために必要だったが、アーキテクチャの変更にあたる。
   実測は取ってあるが、**マージの判断は人間に委ねる。**

5. **提示モードのボタンを 640px 未満でも出すか。**
   いま出していないのは、320px でヘッダーが折り返して別の破綻を生むためである。

6. **`assets/icon-master.png`（1024×1024 の原本）の置き場所。**
   `public/` から出したので配布物には含まれなくなった。アイコンを差し替えるときは
   ここを置き換えて `npm run icons` を走らせる。
